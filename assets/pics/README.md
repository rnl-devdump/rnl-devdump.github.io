# Letter pictures

Upload image files here (`.jpg`, `.png`, `.webp`, etc.).

They are served at `https://<your-site>/assets/pics/<filename>`.

Use the **Letter X** link from the helper and set **Picture 1** / **Picture 2** to the filename only (e.g. `sunset.jpg`).
